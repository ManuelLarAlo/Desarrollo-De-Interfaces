import 'package:flutter/material.dart';
import 'package:practica_login/presentation/screens/Widgets/shared/messagefield_box.dart';

class AfterloginScreen extends StatelessWidget {
  const AfterloginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('After Login Screen'),
      ),
      body: const Text('ge'),
    );
  }
}